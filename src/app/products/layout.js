export const metadata = {
  title: "Premium Wood Cold Pressed Oils",
  description: "Browse our collection of 100% natural, wood cold-pressed edible oils. Pure, healthy, and delivered fresh.",
};

export default function ProductsLayout({ children }) {
  return <>{children}</>;
}
